#include <stdio.h>

/**
 * main - Task 8
 * Description - base 26 lower case
 *
 * Return: 0;
 */
int main(void)
{
	int num;
	char afabets;

	putchar(num);
	}
	for (afabets = 'a'; afabets <= 'f'; afabets++)
	{
	putchar(afabets);
	}
	ptchar('\n');

	return (0);
}
