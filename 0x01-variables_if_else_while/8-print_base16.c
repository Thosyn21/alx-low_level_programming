#include <stdio.h>

/**
 * main - Task 8
 * Description - base 26 lower case
 *
 * Return: 0;
 */
int main(void)
{
	int num;
	char afabets;

	for (num = '0'; num <= '9'; num++)
	{
	putchar(num)
	}
	for (afabets = 'a'; afabets <= 'f'; afabets++)
	{
	putchar(afabets)
	}
	putchar('\n')

	return (0);
}
