#include "main.h"

/**
 * _abs - Task 6
 * Description - computes the absolute value of an integer
 * @r - An integer input
 * Return: Absolute value of r
 */
int _abs(int r)

{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
